import styled from "styled-components";

export const Items = styled.div`
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    column-gap: 8px;
    row-gap: 20px;
    margin: 40px auto;
    width: 95%; 

`;